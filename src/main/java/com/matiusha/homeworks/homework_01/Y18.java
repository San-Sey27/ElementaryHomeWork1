package com.matiusha.homeworks.homework_01;

import static java.lang.Math.*;

public class Y18 {
    public static void main(String[] args) {
        int x = -1;
        double y18 = (abs(7.2 - 10 * x) / pow((x * x + pow(E, x)), 1. / 3)) * atan((4 * (x / 3)) / (sqrt(pow(1.1, 3) + x * x)));
    }
}
