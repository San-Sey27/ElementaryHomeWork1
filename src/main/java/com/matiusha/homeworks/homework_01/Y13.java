package com.matiusha.homeworks.homework_01;

import static java.lang.Math.*;

public class Y13 {
    public static void main(String[] args) {
        int x = -1;
        double y13 = pow(pow(E, -2 + x), 1.5) * 1. / sqrt(x * x + pow(x, 4) + log(abs(x - 3.14)));
    }
}
