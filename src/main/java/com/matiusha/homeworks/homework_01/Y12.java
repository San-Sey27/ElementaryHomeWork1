package com.matiusha.homeworks.homework_01;

import static java.lang.Math.*;

public class Y12 {
    public static void main(String[] args) {
        int x = -1;
        double y12 = log(sqrt(abs(2 - x) + 1.2)) * 1. / (2 + pow(E, -x)) + pow(2 / x, 1. / 3);
    }
}
