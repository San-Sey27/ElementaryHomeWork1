package com.matiusha.homeworks.homework_01;

import static java.lang.Math.*;

public class Y14 {
    public static void main(String[] args) {
        int x = -1;
        double y14 = (sqrt(pow(sin(x / 2), 3)) + pow((pow(E, 1.3 * x) + pow(E, -1.3 * x)), 1. / 3)) * 1. / (x + 5. / 2);
    }
}
